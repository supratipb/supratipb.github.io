::change location of your new repo
git init
git add .
git commit -m "my page"
git config --global user.email supratip.banerjee@yahoo.in
git config --global user.email "Supratip Banerjee"

::create the below <repo_name> in git
::name a new repo for local the below <repo_name> in git
::git remote add <repo> https://github.com/supratipb/<repo_name>.git

git remote add cert https://github.com/supratipb/supratipb.github.io.git
git push cert master  
